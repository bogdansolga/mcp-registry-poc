# Database Configuration (using local PostgreSQL server)
# Create the database with: ./scripts/db/setup-database.sh
DATABASE_URL=postgresql://mcp_registry_admin:zWyM00mZBHGi2IYTrdkPgYPKoOZYpm2ZLg7m67@localhost:5432/mcp_registry

# Authentication
REGISTRY_USERNAME=admin
REGISTRY_PASSWORD=change-this-secure-password

# Application Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development

# MCP Server Credentials (Optional - for real servers)

# Filesystem MCP Server
# No credentials needed - uses local filesystem

# PostgreSQL MCP Server
MCP_POSTGRES_HOST=localhost
MCP_POSTGRES_PORT=5432
MCP_POSTGRES_USER=mcp_user
MCP_POSTGRES_PASSWORD=mcp_password
MCP_POSTGRES_DB=mcp_registry

# JIRA MCP Server
JIRA_URL=https://your-domain.atlassian.net
JIRA_EMAIL=your-email@example.com
JIRA_API_TOKEN=your-jira-api-token

# Context7 MCP Server
CONTEXT7_API_KEY=your-context7-api-key

# File Operations Mock Server
# No credentials needed - mock server
